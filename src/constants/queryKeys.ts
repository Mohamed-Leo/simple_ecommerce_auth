const AuthQueryKeys = {
  currentUser: ["currentUser"],
};

const ProductQueryKeys = {
  getProducts: ["getProducts"],
};

const CartQueryKeys = {
  getCart: ["getCart"],
};

const CategoryQueryKeys = {
  getCategories: ["getCategories"],
};

export { AuthQueryKeys, ProductQueryKeys, CartQueryKeys, CategoryQueryKeys };
